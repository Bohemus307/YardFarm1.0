import React from 'react';
import propTypes from 'prop-types';

const AlertSlider = ({ config }) => {
  
  console.log('config in slider', config);

  return (
    <div>
      sliders
    </div>
  );
};

AlertSlider.propTypes = {
};

export default AlertSlider;
